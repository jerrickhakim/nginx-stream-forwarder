{
  "name": "nginx-stream-auth",
  "version": "1.0.0",
  "description": "RTMP Stream Authentication Service",
  "main": "auth-server.js",
  "type": "module",
  "scripts": {
    "start": "bun run auth-server.js",
    "dev": "bun run --watch auth-server.js"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
